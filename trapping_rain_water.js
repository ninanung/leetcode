/**
 * @param {number[]} height
 * @return {number}
 */
var trap = function(height) {
    function recursive() {
        
    }
    
    if(height.length < 3) return 0;
    const stack = [];
    stack.push(height[0]);
    
};